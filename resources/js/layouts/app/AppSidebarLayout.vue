<script setup lang="ts">
import AppContent from '@/components/legacy/AppContent.vue';
import AppShell from '@/components/legacy/AppShell.vue';
import AppSidebar from '@/components/legacy/AppSidebar.vue';
import AppSidebarHeader from '@/components/legacy/AppSidebarHeader.vue';
import type { BreadcrumbItemType } from '@/types';

interface Props {
    breadcrumbs?: BreadcrumbItemType[];
}

withDefaults(defineProps<Props>(), {
    breadcrumbs: () => [],
});
</script>

<template>
    <AppShell variant="sidebar">
        <AppSidebar />
        <AppContent variant="sidebar">
            <AppSidebarHeader :breadcrumbs="breadcrumbs" />
            <slot />
        </AppContent>
    </AppShell>
</template>
